<Window x:Class="ReInitializeDatabase.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ReInitializeDatabase.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowVm/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12" Orientation="Horizontal" VerticalAlignment="Top">
            <TextBlock Text="Serial Number:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Width="200" Margin="8,0,0,0"
                     Text="{Binding SerialNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Load Files" Width="100" Margin="8,0,0,0"
                    Click="LoadFiles_Click"
                    IsEnabled="{Binding IsLoadEnabled}" />

            <Button Content="Check / Uncheck All" Width="150" Margin="8,0,0,0"
                    Click="SelectAll_Click"
                    IsEnabled="{Binding HasFiles}"/>
            
            <Button Content="Send" Width="100" Margin="8,0,0,0"
                    Click="Send_Click"
                    IsEnabled="{Binding CanSend}"/>
        </StackPanel>


        <ListView Grid.Row="1" ItemsSource="{Binding Files}" Name="FilesList" Margin="12" SelectionMode="Extended">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="36">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="File name" Width="380"
                                    DisplayMemberBinding="{Binding FileName}" />

                    <GridViewColumn Header="Size (bytes)" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FileSize, StringFormat={}{0:N0}}"
                                           HorizontalAlignment="Right"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>

</Window>
